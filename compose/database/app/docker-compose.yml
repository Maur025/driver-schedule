services:

  driver-schedule-db:
    image: ${POSTGRESQL_IMAGE}
    restart: unless-stopped
    hostname: driver-schedule-db
    container_name: driver-schedule-db
    ports:
      - "5540:5432"
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - driver-schedule-db:/var/lib/postgresql/data
      - ./init-user-db.sh:/docker-entrypoint-initdb.d/init-user-db.sh
      - ./dumps:/dumps
    networks:
      - compose-net

volumes:
  driver-schedule-db:
    name: driver-schedule-db

networks:
  compose-net:
    external: true