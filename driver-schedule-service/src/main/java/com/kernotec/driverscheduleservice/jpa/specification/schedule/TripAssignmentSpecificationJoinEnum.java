package com.kernotec.driverscheduleservice.jpa.specification.schedule;

public enum TripAssignmentSpecificationJoinEnum {
    SCHEDULE_TRANSPORTATION_JOIN, SCHEDULE_TRANSPORTATION_STATE_JOIN, TRIP_JOIN, TRIP_STATE_JOIN
}
