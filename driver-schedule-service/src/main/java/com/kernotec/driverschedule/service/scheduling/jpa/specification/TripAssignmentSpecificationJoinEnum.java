package com.kernotec.driverschedule.service.scheduling.jpa.specification;

public enum TripAssignmentSpecificationJoinEnum {
    SCHEDULE_TRANSPORTATION_JOIN, SCHEDULE_TRANSPORTATION_STATE_JOIN, TRIP_JOIN, TRIP_STATE_JOIN, TRIP_ASSIGNMENT_STATE;
}
