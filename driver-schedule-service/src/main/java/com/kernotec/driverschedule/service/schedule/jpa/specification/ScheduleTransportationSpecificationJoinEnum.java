package com.kernotec.driverschedule.service.schedule.jpa.specification;

public enum ScheduleTransportationSpecificationJoinEnum {
    SCHEDULE_TRANSPORTATION_STATE_JOIN,
    TRIP_ASSIGNMENT_JOIN,
    TRANSPORTATION_REQUEST_JOIN,
    TRIP_JOIN,
    TRIP_STATE_JOIN;
}
